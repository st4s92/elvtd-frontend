<form method="post" action="{{ path('app_trade_log_delete', {'id': trade_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trade_log.id) }}">
    <button class="btn">Delete</button>
</form>
