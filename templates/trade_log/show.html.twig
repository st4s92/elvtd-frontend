{% extends 'base.html.twig' %}

{% block title %}TradeLog{% endblock %}

{% block body %}
    <h1>TradeLog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trade_log.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_trade_log_index') }}">back to list</a>

    <a href="{{ path('app_trade_log_edit', {'id': trade_log.id}) }}">edit</a>

    {{ include('trade_log/_delete_form.html.twig') }}
{% endblock %}
